# Azure AD Configuration - Government Cloud (GCC-High)
# Get these from your Azure Government portal at https://portal.azure.us

# Required: Azure AD Application (client) ID
LATCH_CLIENT_ID=your-client-id-here

# Required: Azure AD Directory (tenant) ID  
LATCH_TENANT_ID=your-tenant-id-here

# Required: Azure cloud environment
LATCH_CLOUD=gcc-high

# Required: OAuth scopes (space-separated)
# ⚠️ Use .us endpoints or simple scope names (Latch auto-expands)
# Common patterns:
#   Minimal: openid profile email
#   Standard: openid profile email User.Read
#   Full: openid profile email offline_access User.Read
LATCH_SCOPES=openid profile email User.Read offline_access

# Required: Redirect URI (must match Azure AD app registration)
LATCH_REDIRECT_URI=http://localhost:3000/api/latch/callback

# Required: Cookie encryption secret (generate with: openssl rand -base64 32)
LATCH_COOKIE_SECRET=your-32-byte-secret-here

# Optional: Client secret (for confidential client flow)
# Leave blank for public client (PKCE only)
# LATCH_CLIENT_SECRET=your-client-secret-here

# Optional: Enable debug logging (never logs tokens)
# LATCH_DEBUG=true

# Optional: Security settings
# LATCH_CLOCK_SKEW_TOLERANCE=60    # seconds, default: 60
# LATCH_JWKS_CACHE_TTL=3600         # seconds, default: 3600

# Azure Government Cloud (GCC-High) Endpoints (automatically configured)
# Login: https://login.microsoftonline.us
# Graph API: https://graph.microsoft.us
#
# ⚠️ IMPORTANT: GCC-High requires:
# - Azure Government subscription (IL4 compliance)
# - App registration in Azure Government portal
# - Users must have GCC-High licenses
# - Cannot mix with Commercial cloud
#
# For DoD (IL5), change LATCH_CLOUD=dod
# DoD uses same login endpoint but different Graph: dod-graph.microsoft.us
